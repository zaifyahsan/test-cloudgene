id: test-cloudgene
name: Test Cloudgene
description: Test Application for Cloudgene.<br>This sample application runs PhyloPGM server.
version: 1.0.0
author: faizyahsan
submitButton: Generate PhyloPGM results
workflow:
  steps:

    - name: PhyloPGM results
      type: python
      script: check.py
      quotes: ${input}

  inputs:
    - id: input
      description: GenomicLocations
      type: text
      value: 5
