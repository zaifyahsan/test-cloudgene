id: test-cloudgene
name: Test Cloudgene
description: Test Cloudgene.<br>This application checks phylopgm server.
version: 1.0.0
author: faizy
submitButton: Generate PhyloPGM Scores
workflow:
  steps:

    - name:  ${input} 
      type: bash
      script: check.bash
      quotes: ${input}

  inputs:
    - id: input
      description: How many quotes?
      type: text
      value: chr1 100 200 +
